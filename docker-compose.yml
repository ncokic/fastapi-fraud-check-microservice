services:
  fraud_service:
    build: .
    ports:
      - "8080:8000"
    env_file:
      - .env
    networks:
      - ecommerce_network

networks:
  ecommerce_network:
    external: true
    name: ecommerce_shared_net